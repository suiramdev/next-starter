{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "format": {
      "dependsOn": ["^format"]
    },
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env"],
      "outputs": [".next/**", "!.next/cache/**", "storybook-static/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "env": [
        "NEXT_PUBLIC_BETTER_AUTH_BASE_URL",
        "CORS_ALLOWED_ORIGINS",
        "ZERO_UPSTREAM_DB",
        "ZERO_REPLICA_FILE",
        "ZERO_GET_QUERIES_URL",
        "NEXT_PUBLIC_ZERO_SERVER",
        "ZERO_AUTH_SECRET"
      ]
    },
    "auth:generate": {
      "dependsOn": ["^auth:generate"],
      "env": ["ZERO_AUTH_SECRET", "ZERO_UPSTREAM_DB"]
    },
    "db:generate": {
      "dependsOn": ["^auth:generate", "^db:generate"],
      "env": ["ZERO_UPSTREAM_DB"]
    },
    "db:migrate": {
      "persistent": true,
      "env": ["ZERO_UPSTREAM_DB"]
    },
    "db:studio": {
      "persistent": true,
      "env": ["ZERO_UPSTREAM_DB"]
    },
    "db:reset": {
      "persistent": true,
      "env": ["ZERO_UPSTREAM_DB"]
    },
    "db:deploy": {
      "dependsOn": ["^db:deploy"],
      "env": ["ZERO_UPSTREAM_DB"]
    },
    "db:push": {
      "dependsOn": ["^db:push"],
      "env": ["ZERO_UPSTREAM_DB"]
    },
    "db:seed": {
      "env": ["ZERO_UPSTREAM_DB"]
    }
  }
}
